@model Options.RAMClasses

@{
    var values = Enum.GetValues(typeof(Options.RAMClasses)).Cast<Options.RAMClasses>();

    IEnumerable<SelectListItem> items =
        from value in values
        select new SelectListItem
        {
            Text = value.ToString(),
            Value = value.ToString(),
            Selected = (value.Equals(Options.RAMClasses.DDR4))
        };
}

@Html.DropDownList("RAMClass", items)